import { describe, it } from 'vitest';

describe('constructWhereClause', () => {
	it.todo('should return a where clause object with the correct filter applied');
	it.todo('should return a where clause object with the correct search applied');
	it.todo('should throw an error when an invalid filter is provided');
});

describe('constructOrderClause', () => {
	it.todo('should return an order clause object with the correct sort applied');
});
